@using EntityLayer.Concrete
@model List<Message2>


<a class="nav-link count-indicator dropdown-toggle" id="messageDropdown" href="#" data-toggle="dropdown" aria-expanded="false">
    <i class="mdi mdi-email-outline"></i>
    <span class="count-symbol bg-warning"></span>
</a>
<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="messageDropdown">
    <h6 class="p-3 mb-0">Mesajlarım</h6>
    <div class="dropdown-divider"></div>
    @foreach (var item in Model.Take(3))
    {
    <a class="dropdown-item preview-item">
        
        <div class="preview-thumbnail">
            <img src="~/writerimagefiles/@item.SenderUser.WriterImage" alt="image" class="profile-pic">
        </div>
        <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
            <h6 class="preview-subject ellipsis mb-1 font-weight-normal">@item.SenderUser.WriterName mesaj gönderdi!</h6>
                <p class="text-gray mb-0"> @(((DateTime)item.MessageDate).ToString("dd-MMM-yyyy")) </p>
        </div>
    </a>
        }
    <a href="/Message/InBox/"> <h6 class="p-3 mb-0 text-center">Tüm Mesajları gör</h6></a>
    </div>